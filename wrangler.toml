# Cloudflare Pages Functions 설정
name = "hospital-ai"
pages_build_output_dir = "dist"

# Functions 설정이 루트의 functions/ 폴더에서 자동으로 로드됨
# /api/* 경로는 functions/api/ 폴더의 핸들러로 라우팅됨

# 환경 변수는 Cloudflare Dashboard에서 설정
# Settings > Environment variables > Production & Preview
# - VITE_GEMINI_API_KEY = your_api_key_here

# Service Binding: Rate Limiter Worker 연결
[[services]]
binding = "RATE_LIMITER"
service = "winaid-rate-limiter"

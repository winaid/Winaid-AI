/**
 * Blog Generator — "/blog" 경로
 *
 * TODO: 기존 GenerateWorkspace (contentTab='blog') 연결
 * InputForm + ResultPreview 조합
 */
'use client';

export default function BlogPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-slate-900 mb-4">블로그 생성</h1>
      <p className="text-slate-500">[마이그레이션 대기] GenerateWorkspace 연결 예정</p>
    </div>
  );
}

# FINAL_CHECKLIST.md — 배포 전 최종 검증 체크리스트

> 마지막 업데이트: 2026-03-18

## 자동 검증

- [ ] `npm run build` — 빌드 성공
- [ ] `npx vitest run` — 9파일 110개 테스트 통과
- [ ] 번들 크기 확인 (index < 350KB gzip < 100KB)

## 기능 검증

### 콘텐츠 생성
- [ ] **블로그 생성** — 주제 입력 → 생성 → 결과 표시
- [ ] **카드뉴스 생성** — 3단계 워크플로우 (스크립트 → 프롬프트 → 생성)
- [ ] **보도자료 생성** — 주제 입력 → 생성 → 결과 표시
- [ ] **이미지 생성** — 프롬프트 → 이미지 생성
- [ ] **AI 보정** — 기존 콘텐츠 입력 → 보정 결과

### 결과 처리
- [ ] **저장** — Supabase에 결과 저장
- [ ] **다운로드** — HTML/Word 내보내기
- [ ] **재생성** — 같은 입력으로 다시 생성
- [ ] **히스토리** — 저장된 결과 목록 표시 및 열기

### 인증
- [ ] **회원가입** — 이메일/비밀번호 가입
- [ ] **로그인** — 이메일/비밀번호 로그인
- [ ] **OAuth** — Google OAuth 로그인
- [ ] **로그아웃** — 세션 완전 삭제 + 페이지 리셋
- [ ] **프로필 자동 생성** — 첫 로그인 시 profiles/subscriptions 생성

### 라우팅
- [ ] **랜딩** → `/` 정상 표시
- [ ] **앱** → `/app` 홈 대시보드
- [ ] **블로그** → `/blog` 생성 페이지
- [ ] **카드뉴스** → `/card_news` 생성 페이지
- [ ] **보도자료** → `/press` 생성 페이지
- [ ] **관리자** → `/admin` 관리 페이지
- [ ] **인증** → `/auth` 로그인 페이지
- [ ] **뒤로가기** — 브라우저 뒤로가기 정상 동작
- [ ] **해시 URL 호환** — `#app` → `/app` 리다이렉트

### UI
- [ ] **다크모드** — 토글 + localStorage 유지
- [ ] **사이드바** — 접기/펼치기
- [ ] **모바일** — 반응형 레이아웃
- [ ] **에러 모달** — API 에러 시 모달 표시 + 닫기
- [ ] **의료광고법 검색** — 플로팅 버튼 동작

## 번들 기준값 (2026-03-18)

| Chunk | 크기 | gzip |
|-------|------|------|
| index | 303KB | 96KB |
| vendor-utils | 205KB | 49KB |
| calendarTemplateService | 199KB | 60KB |
| supabase | 168KB | 44KB |
| TemplateGenerator | 145KB | 29KB |
| ResultPreview | 123KB | 31KB |
| templateAiEngine | 113KB | 31KB |
| geminiService | 88KB | 33KB |

## 환경 체크

- [ ] `VITE_SUPABASE_URL` 설정됨
- [ ] `VITE_SUPABASE_ANON_KEY` 설정됨
- [ ] `VITE_GEMINI_PROXY_URL` 설정됨
- [ ] Supabase 프로젝트 활성
- [ ] Gemini 프록시 활성

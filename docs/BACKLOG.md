# BACKLOG

> 사용자가 진행 중에 던지는 후속 TODO를 한 곳에 모아두는 파일.
> handoff 문서(`handoff-YYYY-MM-DD.md`)는 시점별 인수인계용이라 build-up 이 어려워서 분리.
> 새 항목은 위(최신)로 추가. 완료된 항목은 `## ✅ 완료 (날짜)` 섹션으로 이동.

---

## 우선순위 신호

| 신호 | 의미 |
|---|---|
| 🚀 Quick win | 0.5~1일, 기존 인프라 재사용 가능 |
| 🔨 표준 | 1~3일, 신규 코드 또는 신규 테이블 동반 |
| 🏗️ 큰 작업 | 1주+ 또는 외부 의존 / 정책 결정 필요 |

---

## 📌 Pending — 2026-05-08 추가

### A. GEO 진단 페이지 후속 (확장)

| # | 항목 | 규모 | 메모 |
|---|---|---|---|
| A1 | **B2B 영업 자료 — 외부 공유 링크를 영업 도구로 활용** | 🚀 Quick win | next-app 에 `/check/[token]` 외부 공유 인프라가 이미 있음 (handoff §10.3). 영업용으로는 UI 정비·만료 정책·열람 통계 정도만 추가하면 단기 산출 가능 |
| A2 | **이메일 알림 — 점수 변동 시 자동 알림** | 🔨 표준 | 진단 결과 비교 로직 + email 발송(SMTP 또는 Resend 같은 서비스) + 구독자 테이블 필요. 외부 의존 도입 결정 동반 |
| A3 | **순위 모니터링 — AI 실측 결과 추세 (주간 자동 실행)** | 🔨 표준 | Vercel cron(이미 `/api/cron/crawl-all` 패턴 있음) + 누적 저장 테이블 + 추세 차트 UI. ChatGPT/Gemini 실측 비용이 주간 N회로 누적되는 점 정책 결정 필요 |

### B. 신규 기능 / 기존 기능 보강

| # | 항목 | 규모 | 메모 |
|---|---|---|---|
| B1 | **네이버 자동 글쓰기 (블로그 자동 발행)** | 🚀 Quick win~표준 | `winai-blog-publisher/` 로컬 Playwright 앱이 이미 발행 인프라 보유 (localhost:17580). 발행 트리거 UI + 토큰 핸드셰이크 정비 정도면 통합 가능. **단** Naver TOS / 봇 탐지 risk 별도 검토 (handoff §C) |
| B2 | **카드뉴스 · 이미지 생성 기능 보강** | ❓ 정의 필요 | 어떤 보강인지 사용자 추가 입력 대기. 후보: 슬라이드 템플릿 추가 / 캔버스 에디터 단축키 / 이미지 자동 자르기 / 일괄 다운로드 향상 / 새 이미지 카테고리 등 — 시작 전에 1~2 우선순위 픽 필요 |
| B3 | **병원 홈페이지 자동 글쓰기** | 🔨 표준~🏗️ 큰 작업 | 기존 블로그 파이프라인(`packages/blog-core`)을 홈페이지 카피용 프롬프트로 분기. "메인 히어로", "진료과 소개", "원장 인사말" 같은 카피 타입별 템플릿 정의 동반. 산출물 길이·톤이 블로그와 달라 완전 재사용은 어려움. 정책 정의 + 프롬프트 작성이 가장 큰 비용 |

---

## 추천 시작 순서 (가장 빠른 것부터)

> 사용자 요청: "젤 빠른 거부터 해야 함"

| 순서 | 항목 | 사유 |
|---|---|---|
| 1️⃣ | **A1 (B2B 외부 공유 링크 영업 도구화)** | `/check/[token]` 인프라 재사용 — 신규 테이블·외부 의존 0. 1일 안에 산출 가능. 영업 즉효 |
| 2️⃣ | **B1 (네이버 자동 글쓰기 통합)** | `winai-blog-publisher/` 가 이미 있음. 통합·트리거만 추가. 단 Naver TOS risk 검토 1번 |
| 3️⃣ | **A2 (점수 변동 이메일 알림)** | A1 다음에 자연스러움(공유 링크 + 알림이 영업 자료 패키지). 이메일 서비스 도입 결정만 잡으면 빠름 |
| 4️⃣ | **A3 (주간 순위 모니터링)** | cron + 누적 저장. AI 비용 정책 결정 필요해서 살짝 느림 |
| 5️⃣ | **B3 (홈페이지 자동 글쓰기)** | 프롬프트·템플릿 정의가 큰 작업. 위 4건 이후 |
| ❓ | **B2 (카드뉴스·이미지 보강)** | 정의 모호 — 사용자 추가 입력 후 우선순위 재배치 |

**시작 전 필요 결정** (1~3 진행 시):
- A1: 영업용 공유 링크의 만료/재발급 정책, 열람 로그 수집 여부
- B1: Naver TOS risk 수용 범위 (계정 한도/속도/예외 처리)
- A2: 이메일 발송 서비스 (Resend / SendGrid / SMTP 직접 등)

---

## 진행 컨텍스트 — 현재 차단 중

- **S1 (winaid fallback 제거)** Phase A 완료 → 운영자 GUC 회신 + Phase B OK 대기.
- 위 백로그 항목은 **Sprint 1 (보안·법·운영 정리)** 종료 후 착수 자연스러움. 출시 직전이라 신기능보다 audit 미결 정리가 우선.
- 단 A1 은 코드 변경이 아주 작으면 Sprint 1 막판 끼워넣기 가능 — 진행 시 별도 결정.

---

## ✅ 완료

(아직 없음. 항목 마치는 대로 PR/커밋 SHA 와 함께 여기로 이동.)
